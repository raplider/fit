class UsersController < ApplicationController
  def login
    @user = User.new
  end
  
  def new
    @user = User.new
  end
  
  def create
   # @user = User.new(params[:user])
   # if @user.save
   #   session[:id] = @user.id
   #   redirect_to root_path
   # else
   #   render 'new'
   # end    
  end
    
end
